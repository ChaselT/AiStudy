"""阶段 1 · 项目①：CLI 智能助手 · 客户端封装与流式处理

本文件职责：LLM 客户端的创建、后端切换、一次完整对话轮次（含工具调用闭环）的执行。

需要提供的能力：
- 按环境变量创建客户端，支持在 DeepSeek/Qwen 云端与本地 Ollama 间切换
  （复用 `ex03_switch_backend.py` 的思路）
- 流式调用：边收 chunk 边把文本交给上层打印，同时把完整回复拼出来供回填历史
- 工具调用闭环：识别 tool_calls → 交给 tools 层执行 → 结果回传 → 继续下一轮
  （复用 `ex08_function_calling.py` 的闭环）

要求/提示：
- 流式 + 工具调用是本项目最难的一处：流式模式下 tool_calls 的参数是**分片到达**的，
  需要按 index 累积拼接后才能 json 解析。想清楚再动手，卡住了先把非流式闭环跑通
- 循环要有轮次上限，防止模型来回调工具停不下来
- 本文件不 print，把输出交给 main.py（保持职责单一，将来换 GUI 也不用改这里）
"""
