"""阶段 2 · 《Embedding模型选型与本地部署》· 动手任务

任务：选定本阶段的 embedding 模型，并摸清它的边界。
1. `ollama pull bge-m3`，跑通调用，**验证返回向量是否已归一化**（算模长看是不是 1.0）
2. **批量 vs 单条对照**：100 条文本，分别用单条循环和一次批量，记录耗时
3. **截断验证**（本课最重要）：构造一段超过模型上限的长文本，
   故意把关键信息放在**最后**，然后检索它——看是不是真的搜不到
4. 至少再拉一个模型做对比：同一组句子，两个模型的相似度矩阵有什么不同？分数分布呢？
   ⚠️ 2026-08-11 订正：原文推荐的 `gte-multilingual-base` **Ollama 上没有**（要走
      sentence-transformers 才能用）。Ollama 可直接 pull 的候选：
        qwen3-embedding（有 0.6b / 4b / 8b 多个 tag，中英俱强，首选）
        mxbai-embed-large / bge-large / nomic-embed-text（更小更快，适合当对照）
      选 qwen3-embedding:0.6b 做对比最合适——和 bge-m3 体量接近，比得干净
5. 写下你的选型决定和理由

要求/提示：
- 第 2 题**先预热再计时**——首次调用含模型加载和 kernel 编译
  （[[错题与复盘#E48]]：热身成本藏在 prefill 里，冷启动数据能差 400 倍）
- 第 3 题演示的是一种**完全静默的失败**：不报错、不警告，
  后半段内容凭空消失。这是第 5 课定 chunk 大小时的硬约束来源
- 第 4 题别只看"谁分高"，要看**分数分布**：有的模型所有句子都挤在 0.6 以上，
  那它的 0.7 和另一个模型的 0.7 完全不是一回事（回顾上一课的阈值标定）

完成标准：
- 批量/单条的耗时数字（且 load 时间已排除）
- 截断实验能复现出"后半段搜不到"
- 两个模型的分数分布对比 + 一句选型结论
"""
