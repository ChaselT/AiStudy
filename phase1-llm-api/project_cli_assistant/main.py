"""阶段 1 · 项目①：CLI 智能助手 · 入口与对话循环

本文件职责：命令行入口、主对话循环、斜杠命令处理、把各模块粘起来。

项目整体功能要求（详见 [[02-LLM应用开发-MOC]]）：
1. **多轮对话**：维护对话历史，支持连续追问（上下文不丢失），支持 `/clear` 清空历史
2. **流式输出**：回复逐字打印到终端，不是等全部生成完再输出
3. **至少 3 个工具调用**（Function Calling），例如：
   - `get_current_time`：查询当前时间
   - `calculator`：数学计算（安全求值，**不要裸 `eval`**）
   - `read_file` / `search_web`：读本地文件或联网搜索（任选其一）
4. **可切换后端**：通过环境变量在 DeepSeek/Qwen 云端 API 与本地 Ollama 之间切换

加分项：上下文超长时自动摘要压缩；工具并行调用；对话历史持久化到 JSON 文件。

本文件负责的部分：
- 解析启动参数/环境变量，初始化各模块
- `while True` 主循环：读输入 → 调用 llm 层 → 流式打印 → 回填历史
- 斜杠命令：`/clear`、`/exit`，可自行扩展（`/history`、`/model`、`/save`…）
- 顶层异常处理：网络错误、Ctrl+C 要体面退出，不能糊一屏栈

要求/提示：
- 运行方式：`uv run project_cli_assistant/main.py`
- 别把 LLM 调用、工具执行、历史管理的逻辑写进本文件——那是另外三个模块的事，
  本文件只做编排（类比 Controller 不写业务逻辑）
- 完成标准：对照 MOC 的四条功能要求逐条自测通过，并写一份项目 README 留档
"""
