"""阶段 1 · 项目①：CLI 智能助手 · 历史管理（截断/摘要/持久化）

本文件职责：维护 messages 列表的增删改查、控制上下文长度、可选的落盘与恢复。

需要提供的能力：
- 追加消息、清空历史（供 `/clear` 使用，注意 system 消息要保留）
- 截断策略：保留 system + 最近 N 条（复用 `ex02_usage_watch.py` 的 `trim_history`）
- 加分项 1：上下文超长时自动**摘要压缩**——把早期对话交给模型总结成一段摘要，
  用摘要替换掉那批消息（想清楚摘要放在 messages 的哪个位置、以什么角色存在）
- 加分项 2：对话历史持久化到 JSON 文件，下次启动可恢复

要求/提示：
- 截断时小心工具调用消息：assistant 的 tool_calls 和对应的 tool 结果消息必须成对存在，
  截断截到一半会让 API 直接报错——这是本文件最容易踩的坑
- 用 tiktoken 估算 token 数来决定何时触发压缩，比数消息条数准（复用 ex02 的手感）
- 持久化路径别写死绝对路径；存到 `data/` 下（已被根 .gitignore 忽略，聊天记录不进 Git）
"""
