"""阶段 1 · 《多模态视觉API》· 动手任务 1 + 任务 3

任务 1：让模型看懂报错截图。
1. 截一张你 IDE 的报错图（真实报错，别用合成的）
2. 用本地 `qwen3.5:27b`（它原生支持看图）或云端 `qwen3.7-plus` 分析报错原因
3. 打印模型的分析，对照你自己的判断，看它说得对不对

任务 3：两组对比实验。
A. 云端 vs 本地：同一张**图表**图片（柱状图/折线图，带数字标签）分别发给云端和本地模型，
   要求提取图中数据，逐个数字核对准确度
B. 图片大小 vs 成本：**同一张图压缩到 1000px 和保持原图各跑一次**，
   对比 `usage.prompt_tokens` 的差距——体会"图片很贵"
两组结论都写进本文件注释。

要求/提示：
- 图片传参有两种方式：公网 URL 或 base64 data URI；本地图片走 base64
- 完成标准：两个任务都跑通；给出"本地模型在哪类图上够用、在哪类上不行"的结论，
  以及压缩前后的 token 数字对比
"""
