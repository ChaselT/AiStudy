"""阶段 1 · 《多模态视觉API》· 动手任务 1 + 任务 3

任务 1：让模型看懂报错截图。
1. 截一张你 IDE 的报错图（真实报错，别用合成的）
2. 用 qwen-vl-plus（云端）或本地 qwen2.5vl:7b 分析报错原因
3. 打印模型的分析，对照你自己的判断，看它说得对不对

任务 3：云端 vs 本地对比实验。
1. 找一张**图表**图片（柱状图/折线图，带数字标签）
2. 同一张图分别发给云端 qwen-vl-max 和本地 qwen2.5vl:7b，要求提取图中数据
3. 对比数据提取准确度（逐个数字核对），结论写进脚本注释

要求/提示：
- 图片传参有两种方式：公网 URL 或 base64 data URI；本地图片走 base64
- 注意图片也算 token，而且很贵——顺手打印 usage 看看一张图值多少 token
- 完成标准：两个任务都跑通；对比实验给出"本地模型在哪类图上够用、在哪类上不行"的结论
"""
