"""阶段 2 · 《分块策略》· 动手任务

任务：把切分从"拍脑袋"变成"有依据"。
1. 实现 fixed_chunk() 和 recursive_chunk()（**自己写，别调库**），
   在同一份文档上对比切出来的结果
2. 实现 structure_chunk()：基于上一课的 heading_path，
   并把标题路径拼进 chunk 开头
3. **长度分布分析**：三种方法各自的 chunk 长度直方图（用 token 数不是字符数），
   找出异常短的 chunk 并说明原因
   ⚠️ 2026-08-11 订正：原文写"用 tiktoken"，但**这里的硬约束是 bge-m3 的 8192 上限，
      而 tiktoken 是 OpenAI 的分词器，数出来不是 bge-m3 的 token 数**。
      粗估可以用 tiktoken，但要知道它有偏差；想准就用 bge-m3 自己的 tokenizer
      （sentence-transformers 加载后可直接 `model.tokenizer`）。
      在注释里写明你用了哪种、偏差大概多少
4. **边界断裂实验**：构造一段"关键信息横跨两个 chunk 边界"的文本
   （问题在上一段、答案在下一段），验证三组对照：
   - 无 overlap 时能不能检索到？
   - 加了 overlap 呢？
   - 加了 heading_path 前缀呢？
5. 表格处理：找一段 Markdown 表格，验证「按行切但每块带表头」的效果

⚠️ **前置依赖**：第 4 题需要检索能力，但向量库要到第 6 课才学。
   **用 ex01 里那个 numpy 版余弦检索就够了**，别等。

要求/提示：
- chunk 的 token 数**不能超过 embedding 模型上限**——上一课第 3 题你已经
  亲眼见过超限会静默截断
- chunk 大小是有**显存成本**的：召回 5 个 × 每个 1000 token = 5000 token 进上下文，
  而 ex10 实测本地 27b 每 1K 上下文吃 64MB
- 第 4 题是三组对照，**除了要验证的那一个变量，其余一字不动**
  （ex07 的 0.5b 对照做得很干净，照那个来）
- 别一上来就上语义分块。2026 年的建议是：先结构感知 + 适度 overlap，
  **测出了检索质量缺口再上高级方法**

完成标准：
- 三种切分都实现且能对比
- 长度直方图 + 异常 chunk 的解释
- 边界实验有三组对照数据（无 overlap / 有 overlap / 带前缀）
"""
